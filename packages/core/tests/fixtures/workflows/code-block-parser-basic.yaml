id: code-parser-workflow
kind: workflow
version: "1.0"
config:
  model_name: gpt-4o
blocks:
  transform:
    type: code
    code: |
      def main(data):
          return {"out": 1}
workflow:
  name: code_parser_workflow
  entry: transform
  transitions:
    - from: transform
      to: null
