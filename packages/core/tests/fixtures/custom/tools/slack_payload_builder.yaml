version: "1.0"
id: slack_payload_builder
kind: tool
type: custom
executor: python
name: slack_payload_builder
description: Fixture tool for testing.
parameters:
  type: object
  properties: {}
code: |
  def main(args):
      return {}
