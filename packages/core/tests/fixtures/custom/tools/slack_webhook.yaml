version: "1.0"
id: slack_webhook
kind: tool
type: custom
executor: python
name: slack_webhook
description: Fixture tool for testing.
parameters:
  type: object
  properties: {}
code: |
  def main(args):
      return {}
