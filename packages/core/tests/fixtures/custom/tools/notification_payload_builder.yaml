version: "1.0"
id: notification_payload_builder
kind: tool
type: custom
executor: python
name: notification_payload_builder
description: Fixture tool for testing.
parameters:
  type: object
  properties: {}
code: |
  def main(args):
      return {}
