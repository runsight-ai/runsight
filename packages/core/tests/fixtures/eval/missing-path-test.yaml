version: "1.0"
souls:
  default:
    id: default
    model: fixture-model
    system_prompt: "Assistant."
blocks:
  analyze:
    type: llm
    soul: default
    prompt_template: "Analyze."
workflow:
  name: missing_path_test
  entry: analyze
  transitions:
    - from: analyze
      to: END
eval:
  threshold: 1.0
  cases:
    - id: missing_path
      fixtures:
        analyze: '{"data": "value"}'
      expected:
        analyze:
          - type: contains
            value: "value"
            transform: "json_path:$.nonexistent"
